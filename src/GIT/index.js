/*
    git rm -r --cached . : remove cahe
    =====================Add respon =====================a
    git init
    git remote add origin {{url}}
    git pull
    git checkout main -f
    git add .
    git commit -m 'comment'
    git push origin main

*/