/*
    git rm -r --cached . : remove cahe
    =====================Add respon =====================a
    git init
    git remote add origin {{url}}
    git pull
    git checkout main -f
    git add .
    git commit -m 'comment'
    git push origin main
    
    git config --list

    logout terminal
    git config --global --unset-all

    git config --global user.name "vinhDBE"

    git config --global user.email "vinh.lx@vgamestd.com"
    git config --global user.password "Xuanvinh.9411"

    remove config
    git config --global --remove-section user

    vào C:\Users\Admin\.ssh add key id_rsa.pub vào ssh
    
    - check origin url 
    git remote show origin

    -- update file commit thiếu 
    -Tôi vừa lỡ commit sai, làm sao để sửa lại ngay lập tức?
    $ update add file update commit 
    git add README.md
    git commit --amend --no-edit

    $ update commit 
    git commit --amend -m "release v1.0.1"

    $ xóa commit ko reset
    git reset --hard origin/develop
*/